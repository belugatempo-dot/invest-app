# invest-app Environment Variables
# Rename this file to .env.local and fill in your values

# -- Database -------------------------------------------------
# Local dev: omit these to use SQLite file at ./data/invest.db
# Production (Turso): set both for cloud database
# DATABASE_URL=libsql://your-db-name.turso.io
# DATABASE_AUTH_TOKEN=your-turso-auth-token

# -- LLM (optional) ------------------------------------------
# AI thesis generation. Without these, thesis feature returns 503.
# Supported providers: anthropic, openai, deepseek
# LLM_PROVIDER=anthropic
# LLM_API_KEY=sk-ant-api03-...
# LLM_MODEL=claude-sonnet-4-20250514

# -- Authentication (optional) --------------------------------
# If set, all requests must include this token.
# Pass via: Authorization: Bearer <token> or ?token=<token>
# AUTH_TOKEN=your-secret-token

# -- Cron (Vercel auto-sets this) -----------------------------
# CRON_SECRET=auto-generated-by-vercel

# -- Local dev ------------------------------------------------
# PORT=8888
